import { addons } from "@storybook/manager-api";
import { themes } from "@storybook/theming";

addons.setConfig({
  title: "Coriano Harris Web Components Library", // Change the title here
  theme: themes.light, // Optional: Customize theme
});
